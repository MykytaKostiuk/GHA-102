name: 04 - Using Actions
on: 
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          path: '.'
      - name: Printing Folders
        run: |
          ls -lah .
           
          
          